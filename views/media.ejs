<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/css/styles.css">
    <script src="/js/index.js"></script>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <div class="w-full min-h-screen bg-zinc-900 p-10 text-white">
        <div class="links flex gap-4">
            <a class="bg-blue-500 rounded-md px-5 py-2 text-white text-sm inline-block mb-5" href="/profile">Go back to
                profile</a>
            <a class="bg-red-500 rounded-md px-5 py-2 text-white text-sm inline-block mb-5" href="/logout">Logout</a>
        </div>
        <h5 class="text-3xl mt-5">All posts</h5>
        <div class="post-container mt-5 flex gap-5 flex-wrap">
            <% posts.reverse().forEach(function(post){ %>
            <div class="post w-full max-w-md p-4 rounded-md bg-zinc-800 border border-zinc-700 ">
                <h4 class="text-blue-500 mb-1 text-sm">@<%= post.user.username %></h4>
                <p class="tracking-tight"><%= post.content %></p>
                <small class="mt-4 inline-block"><%= post.likes.length %> Likes</small>
                <div class="btns flex gap-4">
                    <a class="text-blue-500" href="/like/<%= post._id %>">
                        <%= post.likes.indexOf(post.user._id) ? 'Like' : 'Unlike' %>
                    </a>
                    <a class="text-zinc-600" href="/edit/<%= post._id %>">Edit</a>
                    <a class="text-red-500" href="/delete/<%= post._id %>">Delete</a>
                </div>
            </div>
            <%}) %>
        </div>
</body>

</html>
